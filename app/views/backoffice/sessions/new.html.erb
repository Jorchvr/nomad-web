<div class="card shadow-sm" style="width: 400px; max-width: 95vw;">
  <div class="card-body p-4 p-md-5">
    <div class="text-center mb-4">
      <span class="wnw-logo wnw-logo-lg d-block mx-auto mb-3"></span>
      <h4 class="fw-bold">World Nomad Web</h4>
      <p class="text-muted">Sign in to your account</p>
    </div>

    <%= form_with url: backoffice_login_path, method: :post, local: true do |f| %>
      <div class="mb-3">
        <%= f.label :email, "Email", class: "form-label fw-semibold" %>
        <%= f.email_field :email, class: "form-control form-control-lg", placeholder: "tu@email.com", autofocus: true %>
      </div>
      <div class="mb-4">
        <%= f.label :password, "Password", class: "form-label fw-semibold" %>
        <%= f.password_field :password, class: "form-control form-control-lg", placeholder: "••••••••" %>
      </div>
      <%= f.submit "Sign in", class: "btn btn-primary btn-lg w-100" %>
    <% end %>

    <hr class="my-4">
    <p class="text-center text-muted mb-0 small">
      Don't have an account?
      <%= link_to "Sign up for free", backoffice_register_path, class: "fw-semibold text-decoration-none" %>
    </p>
    <p class="text-center mt-2">
      <%= link_to "← View directory", root_path, class: "text-muted small" %>
    </p>
  </div>
</div>
